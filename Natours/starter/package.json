{
  "name": "natours",
  "version": "1.0.0",
  "description": "A landing page for an travel service",
  "main": "index.js",
  "scripts": {
   "sass": "sass --style=expanded --source-map sass/main.scss css/style.css",
    "sass:watch": "sass --watch sass/main.scss css/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Artur",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.90.0"
  }
}
